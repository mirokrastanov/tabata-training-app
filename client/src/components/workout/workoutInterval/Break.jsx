import React from 'react';

function Break({}) {
    // take id, order id, and context info if used for edit or view layouts
    // ORDER ID & ID should match its corresponding exercise - to be done when combining them
    // inside the WorkoutInterval component


    return (
        <div>Break</div>
    )
}

export default Break;